services:
  redis:
    image: redis:7-alpine
    container_name: dbuff-redis
    ports:
      - "${REDIS_PORT:-6381}:6379"
    volumes:
      - redis-data:/data
    restart: unless-stopped

volumes:
  redis-data:
